# Copy to .env and adjust values before running `docker compose up -d`
# GOOGLE_API_KEY=
# GOOGLE_API_KEY=
GOOGLE_API_KEY=
GEMINI_MODEL=
AI_MAX_CALLS_PER_MINUTE=

# --- Postgres Configuration ---
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_PORT=

# --- Adminer (web DB UI) ---
ADMINER_PORT=

# --- Application (future use) ---
# DATABASE_URL style (SQLAlchemy / Django) examples:
# DATABASE_URL=postgresql://analysis_user:change_me@localhost:5432/analysis_db
# DJANGO_DB_URL=postgres://analysis_user:change_me@postgres:5432/analysis_db

# AI / Scoring existing vars (if needed by future persistence layer)
# GOOGLE_API_KEY=your_key_here
# GEMINI_MODEL=gemini-2.5-pro
# AI_MAX_CALLS_PER_MINUTE=6
